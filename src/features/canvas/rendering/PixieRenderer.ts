import { Application } from 'pixi.js';

export class PixieRenderer {
  // app: Application;
  private shapes: Shape[] = [];

  setup() {
    debugger;
    new Application({ width: 640, height: 360 });
  }

  // render(data: ObjectData[]) {
  //   data.forEach(data => {

  //   });
  // }
}
